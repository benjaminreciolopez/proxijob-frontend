{
  "buildCommand": "cd frontend && npm install -g pnpm@10.10.0 && pnpm install && pnpm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'skip'",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
